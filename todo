- check if ufw.sh and no postfix works
- check if some nextver, phpver, restoredir cnf work
- check if letsencrypt.sh works
- work with config file for variable (nextcloud version, php version, ssh port, etc)
- check if convert-filecache-bigint (nc-init) is needed
- add mailconfig to config.php

